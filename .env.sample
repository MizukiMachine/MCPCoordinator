################################
# OpenAI / Realtime credentials #
################################
# Never commit live credentials. Replace the placeholders locally only.
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_REALTIME_MODEL=gpt-realtime
OPENAI_REALTIME_TRANSCRIPTION_MODEL=gpt-4o-transcribe
OPENAI_REALTIME_VOICE=marin
NEXT_PUBLIC_REALTIME_MODEL=gpt-realtime
NEXT_PUBLIC_REALTIME_TRANSCRIPTION_MODEL=gpt-4o-transcribe
NEXT_PUBLIC_REALTIME_VOICE=marin

##########################
# BFF / client transport #
##########################
# Shared secret that the Next.js API routes expect for admin endpoints.
BFF_SERVICE_SHARED_SECRET=dev-change-me
# Allows the UI to forward browser logs to the server. Defaults are safe.
NEXT_PUBLIC_CLIENT_LOG_ENDPOINT=/api/client-logs
NEXT_PUBLIC_CLIENT_LOG_MIRROR=true

#############################################
# Google Cloud & Gemini File Search (Future)#
#############################################
# Target project that hosts File Search & Drive connectors.
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
# Path to the service-account JSON with file-search-admin role.
GOOGLE_APPLICATION_CREDENTIALS=./secrets/file-search-admin.json
# Default File Search store to query.
GEMINI_FILE_SEARCH_DATA_STORE=projects/your-gcp-project-id/locations/us/fileStores/default-store
# Optional: Drive folder ID or shared drive ID synced into File Search.
RAG_SOURCE_DRIVE_ID=drive-folder-or-shared-drive-id
