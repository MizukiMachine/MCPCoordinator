# MCPサーバーの定義例（人が読み書きしやすいYAML）
# このファイルを `config/mcp.servers.yaml` にコピーして値を編集してください。
#
# フィールド:
# - id: シナリオ側の scenarioMcpBindings で参照するキー
# - transport: sse | streamable_http | stdio
# - url: SSE / Streamable HTTP のエンドポイント
# - command / args: STDIO を使う場合のプロセスコマンド
# - headers: 任意ヘッダー（例: Authorization）
- id: local-tools
  transport: sse
  url: http://localhost:5173/mcp
  headers:
    Authorization: "Bearer change-me"

- id: docs-search
  transport: streamable_http
  url: https://mcp.example.com/api
  cacheToolsList: true

# Google カレンダー（OSS: nspady/google-calendar-mcp）
# - transport: stdio でローカルプロセス起動。npx 経由で実行します。
# - 事前に GCP で「デスクトップ アプリ」OAuth クライアントを作成し、
#   JSON をダウンロードして `GOOGLE_OAUTH_CREDENTIALS` で指すようにしてください。
- id: google-calendar
  transport: stdio
  command: npx
  args:
    - -y
    - @cocal/google-calendar-mcp
  name: Google Calendar (OSS)
  cacheToolsList: true
  timeoutMs: 15000
